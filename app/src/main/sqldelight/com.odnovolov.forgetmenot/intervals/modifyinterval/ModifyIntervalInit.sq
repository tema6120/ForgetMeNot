createStateIfNotExists:
CREATE TABLE IF NOT EXISTS TempModifyIntervalState (
    intervalId INTEGER NOT NULL,
    intervalNumber INTEGER,
    intervalUnit TEXT NOT NULL CHECK (intervalUnit IN ('hours', 'days', 'weeks', 'months'))
);

cleanState:
DELETE FROM TempModifyIntervalState;

initState:
INSERT INTO TempModifyIntervalState VALUES ?;
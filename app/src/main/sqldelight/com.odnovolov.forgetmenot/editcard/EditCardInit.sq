createTableEditCardState:
CREATE TABLE IF NOT EXISTS TempEditCardState (
    question TEXT NOT NULL,
    answer TEXT NOT NULL
);

cleanTableEditCardState:
DELETE FROM TempEditCardState;

initTableEditCardState:
INSERT INTO TempEditCardState SELECT question, answer FROM TempCurrentExerciseCard;
exerciseCardWithoutQuiz:
SELECT ExerciseCard.id AS exerciseCardId,
       ExerciseCard.cardId,
       Card.deckId,
       Card.answer
FROM ExerciseCard JOIN Card ON ExerciseCard.cardId == Card.id
WHERE testMethod == 2 AND ExerciseCard.id NOT IN (SELECT exerciseCardId FROM Quiz);

getCardsByDeckIds:
SELECT * FROM Card WHERE deckId IN ?;

addQuiz:
INSERT INTO Quiz VALUES ?;
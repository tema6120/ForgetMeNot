CREATE TABLE SharedPronunciationDb (
    pronunciationId INTEGER PRIMARY KEY
);

insert:
INSERT INTO SharedPronunciationDb(pronunciationId) VALUES (?);

selectAll:
SELECT * FROM SharedPronunciationDb;

delete:
DELETE FROM SharedPronunciationDb WHERE pronunciationId = ?;